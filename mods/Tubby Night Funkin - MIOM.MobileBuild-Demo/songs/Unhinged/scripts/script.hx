
function stepHit(curStep) {
        switch(curStep) {
            case 258:
                camZoomingInterval = 2;
            case 386:
                camZoomingInterval = 1;
            case 1409:
                camZoomingInterval = 1;
            case 1568:
                camZoomingInterval = 100;
        }
    }
